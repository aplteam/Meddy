 R←Test_ZZZ_998(stopFlag batchFlag);⎕TRAP;rf;v;gitReadme;v2;ind
⍝ Checks whether the result of `Version` fits to README.md (required by .Git).
 ⎕IO←0 ⋄ ⎕ML←3
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆OK

 rf←##.Meddy

 :If #.FilesAndDirs.IsFile'README.md'
     v←1⊃rf.Version
     gitReadme←'flat'#.APLTreeUtils.ReadUtf8File'README.md'
     →FailsIf 0∊⍴ind←'Latest version is '{(⍴⍺)+#.APLTreeUtils.Where ⍺⍷⍵}gitReadme
     v2←{⍵↑⍨⌊/⍵⍳' ',⎕UCS 9 10 13}#.APLTreeUtils.dlb ind↓gitReadme
     →FailsIf v≢v2
 :EndIf

 R←∆OK
