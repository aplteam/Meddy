# TableEdit

The `TableEdit` class offers mainly a stand-alone table editor for SQL-like tables. However, it also
contains a `Browse` method for viewing table and a method `CreateGrid` that can be used to insert
a grid into any Form or SubForm.
