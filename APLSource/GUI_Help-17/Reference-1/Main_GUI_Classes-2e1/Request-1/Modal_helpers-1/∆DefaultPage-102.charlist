# Modal helpers

This page lists all modal helpers available in the `Request` class.

Modal helpers are functions that create a dialog, hand over control to the user and, after the user has closed the dialog, 
return an appropriate result.

`→[*.AskForDate]`'
`→[*.AskForNumber]`'
`→[*.AskForPassword]`'
`→[*.AskForString]`'
