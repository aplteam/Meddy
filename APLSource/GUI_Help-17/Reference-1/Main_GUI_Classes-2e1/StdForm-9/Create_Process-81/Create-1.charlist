# Create the Form

Syntax:

~~~
n←Create x
~~~

This function creates the form. The right argument can be `⍬ ⍬` in which case just the defaults
will be used.

Instead it can be one or two parameter spaces.

