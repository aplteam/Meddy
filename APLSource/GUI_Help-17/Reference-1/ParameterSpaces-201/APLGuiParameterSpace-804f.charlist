# APLGuiParameterSpace

This class offers a shared method `Create` which will return a simple namespace with no variables
and no refs in it but one method, `∆List`.

This is the most basic way to create a parameter space. `APLGuiGlobalsParms.CreateDefaults` as well
as all the `CreateParms`\* methods of the main classes will call `APLGuiParameterSpace.Create`.

For details regarding `∆List` use:

~~~
]ADOC.Browse APLGuiParameterSpace
~~~

What is →[*.ADOC]?

